import styled from 'styled-components';

export const Title = styled.p`
  font-size: 3.7em;
  font-weight: bold;
  color: white;
`;

export const SubTitle = styled.p`
  font-size: 1.7em;
  font-weight: 500;
  color: white;
  margin: 1.2em 0;
  text-align: justify;
`;
